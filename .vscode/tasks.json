{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "npm",
			"script": "build:wasm",
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"problemMatcher": [],
			"label": "npm: build:wasm",
			"detail": " cd wasm && wasm-pack build --target web --out-dir ../src/wasm && cd .."
		}
	]
}